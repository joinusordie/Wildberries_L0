port: "8000"

db:
  username: "postgres"
  host: "localhost"
  port: "5432"
  dbname: "postgres"
  sslmode: "disable"

nats:
  serverURL: "nats://localhost:4222"
  clusterID: "test-cluster"
  Producer: "producer"
  Subscriber: "subscriber"
  subject: "model"
